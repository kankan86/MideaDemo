<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <!-- <solid android:color="#FFFFFF" /> -->
    <gradient
        android:angle="270.0"
        android:centerColor="#66CE20"
        android:endColor="#456B0C"
        android:startColor="#B8EE66" />

    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="10dip" />

    <stroke
        android:width="1dp"
        android:color="#A2A2A2" />

    <!-- padding：边界的间隔 -->
    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp" />

</shape>